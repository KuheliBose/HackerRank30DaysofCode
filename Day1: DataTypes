#include <iostream>
#include <iomanip>
#include <limits>

using namespace std;

int main() {
    int i = 4;
    double d = 4.0;
    string s = "HackerRank ";
    int j;
    cin >> j;
    double d1 ;
    cin >> d1;
    string s1;
    cin.ignore();
    getline(cin, s1);
    cout << i + j << endl;
    cout << fixed << setprecision(1)<<(d + d1)<< endl;
    cout << (s + s1) << endl;
